# Types
type Task implements TimeStamps {
  id: ID!
  name: String!
  createdAt: Date!
  updatedAt: Date!
}

type Tasks {
  info: Info!
  results: [Task]!
}

# Inputs
input TaskCreateInput {
  name: String!
}

input TaskUpdateInput {
  name: String
}
